/**
 * SportsProductRow is a thin alias for the generic ProductRow component.
 * Use ProductRow directly for new sections (Trending, New Arrivals, etc.).
 */
export { default } from "@/components/ui/core/ProductRow";
